todo later: simple cpp classes w/ getters and setters that wrap around the struct

class TestStruct {
    public:
        TestStruct() {
            lmcp_init_TestStruct(&ts);
            }

            ~TestStruct() {
                lmcp_free_TestStruct(&ts);
            }

            static bool isTestStruct(lmcp_object *o) {
                return o->type == (constant);
            }

            uint32_t getField() {
                return ts->field;
            }

            uint32_t pack()


    private:
    TestStruct* ts;
